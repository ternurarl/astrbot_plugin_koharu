{
  "koharu_api_base_url": {
    "description": "Koharu API 地址",
    "type": "string",
    "default": "http://koharu-headless:4000/api/v1",
    "hint": "Koharu HTTP API 位于 /api/v1。可填写 http://host:port 或 http://host:port/api/v1。"
  },
  "target_language": {
    "description": "默认目标语言",
    "type": "string",
    "default": "zh-CN",
    "hint": "BCP47 语言码（zh-CN 简体中文 / zh-TW 繁体中文 / en-US 等），持久化到服务端 pipeline.translation.target_language。兼容旧文案（Simplified Chinese、Japanese 等自动映射）。注意：非空默认值 zh-CN 是声明值，会在启动/翻译前强制对齐服务端；要恢复服务端自行决定请留空。"
  },
  "pipeline_steps": {
    "description": "Pipeline 步骤覆盖",
    "type": "string",
    "default": "full",
    "hint": "full 表示全部阶段（0.66 固定阶段流水线）。也可写逗号分隔的 0.66 阶段名 detection/ocr/translation/inpainting；legacy 0.61 步骤名（detector/ocr/translator 等）会自动映射。留空则从 Koharu /config 读取。"
  },
  "system_prompt": {
    "description": "翻译系统提示词",
    "type": "text",
    "default": "",
    "hint": "非空时持久化到服务端 pipeline.translation.instructions（启动时/翻译前应用）。留空不覆盖服务端现有值。"
  },
  "auto_load_llm": {
    "description": "自动加载 Koharu LLM",
    "type": "bool",
    "default": false,
    "hint": "0.66 模型翻译时懒加载，PUT /llm/current 返回 204 即完成。服务端已选好翻译模型时保持关闭。"
  },
  "llm_kind": {
    "description": "LLM 目标类型",
    "type": "string",
    "default": "provider",
    "options": [
      "provider",
      "local"
    ],
    "labels": [
      "服务商模型",
      "本地模型"
    ],
    "hint": "provider 使用 providerId + modelId；local 只使用 modelId。"
  },
  "llm_provider_id": {
    "description": "LLM 服务商 ID",
    "type": "string",
    "default": "openai-compatible",
    "options": [
      "openai",
      "gemini",
      "claude",
      "deepseek",
      "deepl",
      "google-translate",
      "caiyun",
      "openai-compatible"
    ],
    "hint": "仅当 llm_kind 为 provider 时使用。"
  },
  "llm_model_id": {
    "description": "LLM 模型 ID",
    "type": "string",
    "default": "",
    "hint": "留空则跳过自动加载。需与 Koharu /llm/catalog 中展示的模型 ID 一致。"
  },
  "llm_temperature": {
    "description": "翻译 generation temperature",
    "type": "float",
    "default": -1.0,
    "hint": "持久化到服务端 pipeline.translation.generation.temperature（自动钳制 0-2）。填 -1、0 或清空均表示不覆盖服务端现有值。"
  },
  "llm_max_tokens": {
    "description": "翻译 generation 最大 token 数",
    "type": "int",
    "default": 0,
    "hint": "持久化到服务端 pipeline.translation.generation.max_tokens（自动钳制到 u32 上限）。填 0 或清空表示不覆盖服务端现有值。"
  },
  "llm_custom_system_prompt": {
    "description": "LLM 自定义系统提示词",
    "type": "text",
    "default": "",
    "hint": "加载 /llm/current 时传入的可选 instructions。"
  },
  "wait_image_timeout_seconds": {
    "description": "等待图片超时时间（秒）",
    "type": "int",
    "default": 120
  },
  "koharu_ready_timeout_seconds": {
    "description": "Koharu 就绪超时时间（秒）",
    "type": "int",
    "default": 60
  },
  "pipeline_timeout_seconds": {
    "description": "Pipeline 运行超时时间（秒）",
    "type": "int",
    "default": 900
  },
  "operation_poll_interval_seconds": {
    "description": "任务轮询间隔（秒）",
    "type": "int",
    "default": 2
  },
  "http_timeout_seconds": {
    "description": "HTTP 读取超时时间（秒）",
    "type": "int",
    "default": 120
  },
  "http_connect_timeout_seconds": {
    "description": "HTTP 连接超时时间（秒）",
    "type": "int",
    "default": 10
  },
  "max_images_per_request": {
    "description": "单次最大输入图片数",
    "type": "int",
    "default": 20,
    "hint": "填 0 表示插件侧不限制。"
  },
  "max_send_images": {
    "description": "最多返回图片数",
    "type": "int",
    "default": 0,
    "hint": "填 0 表示返回全部渲染图片。"
  },
  "queue_depth": {
    "description": "翻译请求队列深度",
    "type": "int",
    "default": 3,
    "hint": "同时等待翻译的最大请求数。并发固定为 1，超出队列深度的请求将被直接拒绝。填 0 表示不允许排队，仅当前一个翻译完成后才接受新请求。"
  },
  "compress_return_images": {
    "description": "压缩返回图片",
    "type": "bool",
    "default": false,
    "hint": "启用后会在发送前将翻译结果转码压缩，降低触发聊天平台文件大小上限的概率。"
  },
  "return_image_format": {
    "description": "返回图片格式",
    "type": "string",
    "default": "webp",
    "options": [
      "webp",
      "jpg"
    ],
    "labels": [
      "WebP",
      "JPG"
    ],
    "hint": "仅在启用压缩返回图片时生效。JPG 不支持透明通道，会使用白色背景合成。"
  },
  "return_image_quality": {
    "description": "返回图片压缩质量",
    "type": "int",
    "default": 85,
    "hint": "仅在启用压缩返回图片时生效，范围 1-100，数值越低文件越小但画质越差。"
  },
  "close_project_after_export": {
    "description": "导出后关闭 Koharu 项目",
    "type": "bool",
    "default": true
  },
  "delete_project_after_export": {
    "description": "导出后删除 Koharu 项目",
    "type": "bool",
    "default": true,
    "hint": "关闭项目后再调用 DELETE /projects/{name} 彻底删除项目文件夹，避免 Koharu 端残留。"
  },
  "result_retention_policy": {
    "description": "翻译结果缓存策略",
    "type": "string",
    "default": "days",
    "options": [
      "days",
      "forever",
      "none"
    ],
    "labels": [
      "按天保留",
      "永久保留",
      "不保留"
    ],
    "hint": "控制 data/plugin_data/astrbot_plugin_koharu/outputs/ 下翻译结果图片的清理方式。"
  },
  "result_retention_days": {
    "description": "翻译结果保留天数",
    "type": "int",
    "default": 7,
    "hint": "仅当缓存策略为“按天保留”时生效。填 0 表示发送后下一次清理时删除。"
  },
  "pipeline_ocr_model": {
    "description": "OCR 模型",
    "type": "string",
    "default": "",
    "options": [
      "",
      "baberu-ocr",
      "manga-ocr",
      "paddleocr-vl-1.6"
    ],
    "labels": [
      "不覆盖（用服务端现有）",
      "baberu-ocr",
      "manga-ocr",
      "paddleocr-vl-1.6"
    ],
    "hint": "持久化到服务端 pipeline.ocr.model。paddleocr-vl-1.6 依赖 llama.cpp，--gpu 模式下不可用。"
  },
  "pipeline_inpainting_model": {
    "description": "修补（inpainting）模型",
    "type": "string",
    "default": "",
    "options": [
      "",
      "lama",
      "aot-inpainting",
      "flux2-klein",
      "rorem-mixed"
    ],
    "labels": [
      "不覆盖（用服务端现有）",
      "lama",
      "aot-inpainting",
      "flux2-klein",
      "rorem-mixed"
    ],
    "hint": "持久化到服务端 pipeline.inpainting.model。flux2-klein/rorem-mixed 需配合下方的 prompt 配置。"
  },
  "pipeline_inpainting_prompt": {
    "description": "修补模型 prompt",
    "type": "text",
    "default": "",
    "hint": "持久化到服务端 processor 的 flux2-klein/rorem-mixed prompt。留空不覆盖服务端现有值。"
  },
  "pipeline_inpainting_negative_prompt": {
    "description": "修补模型 negative prompt（rorem-mixed）",
    "type": "text",
    "default": "",
    "hint": "持久化到服务端 processor.rorem-mixed.negative_prompt。留空不覆盖服务端现有值。"
  },
  "pipeline_detection_text_threshold": {
    "description": "检测文本置信度阈值",
    "type": "float",
    "default": -1.0,
    "hint": "持久化到服务端 processor.koharu-layout-rfdetr-seg-2xl.text_threshold，自动钳制 0-1。填 -1、0 或清空表示不覆盖服务端现有值。"
  },
  "pipeline_detection_bubble_threshold": {
    "description": "检测气泡置信度阈值",
    "type": "float",
    "default": -1.0,
    "hint": "持久化到服务端 processor.koharu-layout-rfdetr-seg-2xl.bubble_threshold，自动钳制 0-1。填 -1、0 或清空表示不覆盖服务端现有值。"
  },
  "pipeline_detection_panel_threshold": {
    "description": "检测分镜置信度阈值",
    "type": "float",
    "default": -1.0,
    "hint": "持久化到服务端 processor.koharu-layout-rfdetr-seg-2xl.panel_threshold，自动钳制 0-1。填 -1、0 或清空表示不覆盖服务端现有值。"
  },
  "translation_provider": {
    "description": "翻译提供商",
    "type": "string",
    "default": "deepseek",
    "options": [
      "local",
      "atlas-cloud",
      "openai",
      "gemini",
      "claude",
      "deepseek",
      "openai-compatible",
      "openrouter",
      "lm-studio",
      "deepl",
      "google-cloud-translation",
      "caiyun"
    ],
    "hint": "持久化到服务端 pipeline.translation.model.provider。local 仅 --gpu 关闭的全量模式下可用。非空默认值 deepseek 是声明值，会强制对齐服务端（含 quantization/vision 一并重置）；要恢复服务端自行决定请连同 translation_model 一起留空。"
  },
  "translation_model": {
    "description": "翻译模型 ID",
    "type": "string",
    "default": "deepseek-v4-flash",
    "hint": "持久化到服务端 pipeline.translation.model.model。与 /llm/catalog 中展示的模型 ID 一致。"
  },
  "translation_quantization": {
    "description": "翻译模型量化（local 用）",
    "type": "string",
    "default": "",
    "hint": "持久化到服务端 pipeline.translation.model.quantization。仅 local 提供商需要（如 Q4_K_XL），远程提供商留空。"
  },
  "translation_vision": {
    "description": "翻译模型多模态（vision）",
    "type": "bool",
    "default": false,
    "hint": "持久化到服务端 pipeline.translation.model.vision。多模态（能看图）的模型设为 true，纯文本服务商（如 deepseek）保持 false。"
  },
  "openai_compatible_base_url": {
    "description": "openai-compatible 提供商 base URL",
    "type": "string",
    "default": "",
    "hint": "自定义翻译端点（openai-compatible）。填了端点或下方 key 即自动把翻译提供商切到 openai-compatible（无需改 translation_provider），配合 translation_model 使用。需为合法 URL，留空不覆盖服务端现有值。"
  },
  "openai_compatible_api_key": {
    "description": "openai-compatible API Key",
    "type": "string",
    "default": "",
    "hint": "自定义翻译端点的密钥，启动时/翻译前自动 PUT 到服务端 keyring（容器重启即丢，插件自动重放）。优先级高于 provider_secrets 里的 openai-compatible 项。明文存储。"
  },
  "openai_compatible_vision": {
    "description": "openai-compatible 提供商 vision",
    "type": "bool",
    "default": false,
    "hint": "持久化到服务端 providers.openai-compatible.vision。多模态（能看图）的兼容端点设为 true，同时建议把 translation_vision 也设为 true。bool 无法留空，默认 false（与服务端默认一致）。"
  },
  "lm_studio_base_url": {
    "description": "lm-studio 提供商 base URL",
    "type": "string",
    "default": "",
    "hint": "持久化到服务端 providers.lm-studio.base_url，需为合法 URL（否则跳过覆盖）。留空不覆盖服务端现有值。"
  },
  "deepl_base_url": {
    "description": "deepl 提供商 base URL",
    "type": "string",
    "default": "",
    "hint": "持久化到服务端 providers.deepl.base_url，需为合法 URL（否则跳过覆盖）。留空不覆盖服务端现有值。"
  },
  "font_families": {
    "description": "渲染字体族（逗号分隔）",
    "type": "string",
    "default": "CCWildWords,Adobe 黑体 Std",
    "hint": "持久化到服务端 typesetting.font_families（0.66 的默认字体配置）。非空默认值会强制对齐服务端；留空则不覆盖服务端现有值。"
  },
  "provider_secrets": {
    "description": "提供商 API 密钥（容器重启后自动重放）",
    "type": "object",
    "default": {},
    "items": {
      "local": {
        "type": "string",
        "default": "",
        "description": "local 提供商密钥"
      },
      "atlas-cloud": {
        "type": "string",
        "default": "",
        "description": "atlas-cloud 提供商密钥"
      },
      "openai": {
        "type": "string",
        "default": "",
        "description": "openai 提供商密钥"
      },
      "gemini": {
        "type": "string",
        "default": "",
        "description": "gemini 提供商密钥"
      },
      "claude": {
        "type": "string",
        "default": "",
        "description": "claude 提供商密钥"
      },
      "deepseek": {
        "type": "string",
        "default": "",
        "description": "deepseek 提供商密钥"
      },
      "openai-compatible": {
        "type": "string",
        "default": "",
        "description": "openai-compatible 提供商密钥"
      },
      "openrouter": {
        "type": "string",
        "default": "",
        "description": "openrouter 提供商密钥"
      },
      "lm-studio": {
        "type": "string",
        "default": "",
        "description": "lm-studio 提供商密钥"
      },
      "deepl": {
        "type": "string",
        "default": "",
        "description": "deepl 提供商密钥"
      },
      "google-cloud-translation": {
        "type": "string",
        "default": "",
        "description": "google-cloud-translation 提供商密钥"
      },
      "caiyun": {
        "type": "string",
        "default": "",
        "description": "caiyun 提供商密钥"
      }
    },
    "hint": "非空项会在启动时和每次翻译前 PUT /config/providers/{id}/secret 重放到服务端 keyring（容器重启即丢）。密钥以明文存储在插件配置中。"
  }
}
